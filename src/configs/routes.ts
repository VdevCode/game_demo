const routes: { [key: string]: string } = {
  home: '/',
  addData: '/addData',
  choiceJob: '/choiceJob',
  choiceCharactor: '/choiceCharactor',
  play: '/play',
  caculate: '/caculate',
};

export default routes;
