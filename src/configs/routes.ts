const routes: { [key: string]: string } = {
  home: '/',
  register: '/register',
  choiceJob: '/choiceJob',
  choiceCharactor: '/choiceCharactor',
  play: '/play',
  ranking: '/ranking',
};

export default routes;
